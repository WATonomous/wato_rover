control_node:
  ros__parameters:
    path_topic: "/path"
    odom_topic: "/odom/filtered"
    cmd_vel_topic: "/cmd_vel"

    lookahead_distance: 1.0 # m

    max_steering_angle: 1.2 # rad
    linear_velocity: 3.0 # m/s
